<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
         "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="page">
    <sql id="pageSelectList">
		SELECT T.* FROM (
	</sql>
	
	<sql id="pageWhereList">
		) T LIMIT #{offset},#{limit}
	</sql>
	
	<sql id="pageSelectCntFirst">
	    SELECT COUNT(*) FROM (
	</sql>
	
	<sql id="pageSelectCntLast">
	    )
	</sql>
	
	
</mapper>